{
  "name": "multi-tool",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspace shared && npm run build --workspace client && cp -r client/dist server/",
    "start": "npm run start --workspace server"
  }
}
